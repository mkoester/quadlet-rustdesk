[Unit]
Description=RustDesk backup (data directory)

[Service]
Type=oneshot
# Backup the data directory (keys + peer database) using rsync
ExecStart=/usr/bin/rsync -a --delete %h/data/ /var/backups/rustdesk/
